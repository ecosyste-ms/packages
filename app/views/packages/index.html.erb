<div class="container-sm">
  <h2>
    <%= @registry.name %> packages
  </h2>

  <% if @recent_versions.any? %>
    <div class='my-3'>
      <%= line_chart @recent_versions, thousands: "," %>
    </div>
  <% end %>

  <%= render @packages %>
  <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>  
</div>

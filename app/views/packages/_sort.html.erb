<li class="nav-item ms-auto">
  <div class="dropdown">
    <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
      Sort
    </button>
    <ul class="dropdown-menu">
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'name' %>" href="<%= url_for(request.query_parameters.merge(sort: 'name', order: 'asc', keyword: params[:keyword])) %>">Name</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'versions_count' %>" href="<%= url_for(request.query_parameters.merge(sort: 'versions_count', order: 'desc', keyword: params[:keyword])) %>">Versions</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'latest_release_published_at' && params[:order] == 'desc' %>" href="<%= url_for(request.query_parameters.merge(sort: 'latest_release_published_at', order: 'desc', keyword: params[:keyword])) %>">Latest Release</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'latest_release_published_at' && params[:order] == 'asc' %>" href="<%= url_for(request.query_parameters.merge(sort: 'latest_release_published_at', order: 'asc', keyword: params[:keyword])) %>">Oldest Release</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'dependent_packages_count' %>" href="<%= url_for(request.query_parameters.merge(sort: 'dependent_packages_count', order: 'desc', keyword: params[:keyword])) %>">Dependent packages</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'dependent_repos_count' %>" href="<%= url_for(request.query_parameters.merge(sort: 'dependent_repos_count', order: 'desc', keyword: params[:keyword])) %>">Dependent repositories</a></li>
      <li><a class="dropdown-item <%= 'active' if params[:sort] == 'downloads' %>" href="<%= url_for(request.query_parameters.merge(sort: 'downloads', order: 'desc', keyword: params[:keyword])) %>">Downloads</a></li>
    </ul>
  </div>
</li>